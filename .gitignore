.*.swp
*.log
*.orig
*.pyc
__pycache__/

/*.pdf
/android/.gradle
/android/.kotlin
/fastlane/.bundle
/fastlane/.vendor
# fastlane writes a JUnit-style run report after every invocation; it is a
# build artifact (fresh timestamps each run), not a source file.
/fastlane/report.xml

/android/app/build
/android/app/src/main/res/raw*/copyright.md
/android/app/src/main/res/raw*/usersguide.md
/android/guides.d
/android/build
/short

# Release signing material and Play upload credentials — SECRET, never commit.
# The committed templates are android/keystore.properties.example and the
# SUPPLY_JSON_KEY note in fastlane/Appfile.
/android/keystore.properties
/fastlane/play-store-credentials.json
