Fixed: importing a backup with "Replace" now really replaces the drink list — the built-in preset drinks are no longer kept alongside the imported ones, so the catalogue afterwards matches the backup exactly. The language list now offers "(System)", so a fixed language can be switched back to following the device language. And the overflow menu's About screen shows the version, the GPL notice and the licenses of the app's components.
